import { Config } from 'react-redux-firebase-auth';
import { theme } from './theme';

const reactReduxFirebaseAuthConfig: Config = {
  theme,
  createProfile: true,
};

export default reactReduxFirebaseAuthConfig;
